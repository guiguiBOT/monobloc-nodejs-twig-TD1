<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <main>
    <p>{{texte1}}</p>
    <p>{{texte2 | upper}}</p>
    <p>{{texte3 | length}}</p>
    <p>{{texte4 | default('texte par défaut')}}</p>
    <p>{{ maDate|date("d/m/Y") }}</p>
    </main>
</body>
</html>